parameters:
    hostname: rabbit:5672
    database_host: database.local
    database_user: root
    database_password: 1234
    database_name: test
    flows.version: 0
    rabbitmq.hostname: 'tcp://rabbit:61613'
    rabbitmq.username: guest
    rabbitmq.password: guest
    rabbitmq.vhost: /
    rabbitmq.read_timeout: 15
    rabbitmq.connection_timeout: 30
    rabbitmq.prefetch_count: null
    rabbitmq.heartbeat: 60

