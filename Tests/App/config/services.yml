services:
  doctrine:
    class: Symfony\Bridge\Doctrine\RegistryInterface
    synthetic: true
    public: true

  cache.app:
    class: Symfony\Component\Cache\Adapter\ArrayAdapter
    public: true
    calls:
      - [ setLogger, [ '@logger' ] ]